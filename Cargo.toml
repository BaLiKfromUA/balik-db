[package]
name = "balik-db"
version = "0.1.0"
edition = "2024"
description = "A toy DBMS — fishy but functional"

[[bin]]
name = "balik-cli"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"
